// Program for sum of two numbers (with flowchart)


#include <stdio.h>

int main() {
    int a, b, sum;

    printf("Enter two numbers: \n");
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("Sum = %d\n", sum);

    return 0;
}